# SQL

SQL is optional. These examples demonstrate a generic transformation pattern only.

Never commit real server names, database names, credentials or production schema details.

Files:
- `data-contract.sql` — example logical tables
- `latest-per-hour.sql` — deduplication pattern
- `daily-summary.sql` — illustrative aggregation

Validate syntax and performance locally before use.
