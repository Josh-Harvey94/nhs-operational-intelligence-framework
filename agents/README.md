# Agents

- `info-manager/` — structured operational data intelligence
- `system-grip/` — operational assurance, actions and follow-through

Each folder contains a public-safe instruction template and a test checklist.

Replace bracketed placeholders locally. Do not put private values back into the public repository.
